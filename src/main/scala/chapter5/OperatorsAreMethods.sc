val sum = 1 + 2
val sumMore = 1.+(2)
val longSum = 1 + 2L

val s = "Hello, world!"
s indexOf 'o'

s indexOf ('o', 5)

// scala have infix (two operands) and prefix/postfix (unary)
-2.0
(2.0).unary_-

// only +,-,! and ~ can be used as prefix operators (you need to define unary_+/-/!/~)

// in scala you include parentheses for methods with side effects and leave them off otherwise

s.toLowerCase
s toLowerCase
